import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export function useMaxBrightness(disabled?: boolean = false) {
  return (
    <View>
      <Text>UseMaxBrightness</Text>
    </View>
  )
}

export default useMaxBrightness
